naftiko: 1.0.0-alpha2
info:
  label: Fortify Software Security Center API — Issue Selectors
  description: 'Fortify Software Security Center API — Issue Selectors. 1 operations. Lead operation: Fortify Get issue selector
    set. Self-contained Naftiko capability covering one Fortify business surface.'
  tags:
  - Fortify
  - Issue Selectors
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    FORTIFY_API_KEY: FORTIFY_API_KEY
capability:
  consumes:
  - type: http
    namespace: software-security-center-issue-selectors
    baseUri: ''
    description: Fortify Software Security Center API — Issue Selectors business capability. Self-contained, no shared references.
    resources:
    - name: projectVersions-parentId-issueSelectorSet
      path: /projectVersions/{parentId}/issueSelectorSet
      operations:
      - name: getprojectversionissueselectorset
        method: GET
        description: Fortify Get issue selector set
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.FORTIFY_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: software-security-center-issue-selectors-rest
    port: 8080
    description: REST adapter for Fortify Software Security Center API — Issue Selectors. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/projectversions/{parentid}/issueselectorset
      name: projectversions-parentid-issueselectorset
      description: REST surface for projectVersions-parentId-issueSelectorSet.
      operations:
      - method: GET
        name: getprojectversionissueselectorset
        description: Fortify Get issue selector set
        call: software-security-center-issue-selectors.getprojectversionissueselectorset
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: software-security-center-issue-selectors-mcp
    port: 9090
    transport: http
    description: MCP adapter for Fortify Software Security Center API — Issue Selectors. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: fortify-get-issue-selector-set
      description: Fortify Get issue selector set
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: software-security-center-issue-selectors.getprojectversionissueselectorset
      outputParameters:
      - type: object
        mapping: $.
